{
  "version": 2,
  "builds": [
    {
      "src": "*.ts",
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "tsc --build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
