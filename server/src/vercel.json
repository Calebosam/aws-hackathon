{
  "version": 2,
  "installCommand": "npm install",
  "builds": [
    {
      "src": "*.ts",
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "tsc --build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
